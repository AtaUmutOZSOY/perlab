export interface CreateCollaborationRequestDto {
    collaborationName: string,
    collaborationWebSiteLink: string,
    imageBase64String: string // Ensure this matches the backend expected field
  }
  